// Place your settings in this file to overwrite default and user settings.
{
    "less.compile": {
        "main": "${workspaceRoot}/main.less",
        "out": "${workspaceRoot}/Iris_generated.css"
    }
}